# Copy to android/keystore.properties and fill in. That file is gitignored.
# These are the values you set when you generated keystore.jks.
#
# Forgotten the alias? list it with:
#   keytool -list -v -keystore android/keystore.jks
storePassword=
keyAlias=
keyPassword=
# storeFile=keystore.jks
