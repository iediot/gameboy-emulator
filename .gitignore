build-xcode/
build-ios/
build-ios-device/
cmake-build-debug/
.claude/
.DS_Store

# android build output and machine specific sdk path
android/.gradle/
android/build/
android/app/build/
android/app/.cxx/
android/local.properties
android/.idea/
android/keystore.jks
android/keystore.properties

# audio captures from test runs
*.wav

# roms stay on disk but never in the repo, the test-roms submodule is only a pointer
# so it is carved back out
roms/*
!roms/test-roms

# packaged build artifacts, the manifest beside them is committed instead
releases/
build-release/
build-ios-rel/
CLAUDE.md
